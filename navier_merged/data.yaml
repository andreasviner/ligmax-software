# Consolidated Navier Njord buoy dataset (train/valid/test merged)
# Images downscaled to max 1280px long side, re-encoded JPEG q90.
path: .
train: images
val: images

nc: 3
# cardinal = north/east/south/west merged; differentiated downstream, not by the detector
names: ['green','red','cardinal']
