# =============================================================================
# Njord buoy detector — YOLO26 size-comparison training deps
# Target box: Windows 11, Python 3.14, RTX 4090 (24 GB), CUDA already installed
# =============================================================================
#
# INSTALL ORDER MATTERS:
#
#   1) Install PyTorch + torchvision MANUALLY first, matched to your CUDA.
#      The pip wheels bundle their own CUDA runtime, so only your GPU *driver*
#      has to be recent enough — the CUDA toolkit version need not match exactly.
#      Example (pick the cuXXX tag that matches / is <= your setup):
#
#        pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#      HEADS-UP (Python 3.14): confirm a cp314 wheel exists on that index.
#      Python 3.14 is new; if there is no stable cp314 wheel yet, either use the
#      nightly channel  ( .../whl/nightly/cu128 )  or run training on Python 3.12/3.13.
#      Verify after install:
#        python -c "import torch; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#
#   2) THEN install the rest:
#        pip install -r requirements.txt
#
# torch / torchvision are intentionally NOT pinned below so this file will not
# drag in the CPU-only PyPI build on top of your manual CUDA install.
# =============================================================================

# Training + eval + FLOPs/params reporting. Pulls in its own deps (numpy,
# opencv-python, pillow, pyyaml, scipy, matplotlib, pandas, tqdm, psutil,
# py-cpuinfo, ultralytics-thop). If pip can't resolve on Python 3.14, bump this.
ultralytics>=8.4.14
